<UserControl x:Class="Cloudoh.UserControls.CloudohTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="336" d:DesignWidth="336">
    
    <Grid x:Name="LayoutRoot" Background="Black">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="336"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="336"></RowDefinition>
        </Grid.RowDefinitions>
        
        <Image x:Name="image" 
               Grid.Row="0" 
               Grid.Column="0" 
               Width="336"
               Height="336"
               Stretch="UniformToFill"/>

        <StackPanel Background="#225500" Height="80" Width="80" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right">
            <Image Source="/ApplicationIcon.png" Margin="9,0,0,0" Width="62" Height="62" HorizontalAlignment="Center"></Image>
        </StackPanel>

    </Grid>
</UserControl>